namespace exemploPOO.Interfaces
{
    public interface ICalculadora
    {
         int Somar (int first, int second);
         int Subtrair(int first, int second);
         int Multiplicar(int first,int second);
         int Dividir(int first, int second);
    }
}